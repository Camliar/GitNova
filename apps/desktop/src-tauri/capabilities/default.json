{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability for the main GitNova Desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
